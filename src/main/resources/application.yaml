spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql
    username: root
    password: ps
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

jwt:
  secret: secret.key


